================================================================================
PHẦN 1 – HTML: FLASH DEAL STRIP (trong index.html)
================================================================================

<!-- 6. Flash Deal Strip -->
<section class="flash-deal-section reveal">
    <div class="container">
        <div class="flash-deal-wrapper">
            <div class="flash-deal-header">
            <div class="flash-header-left">
                <i class="fas fa-bolt flash-bolt-icon"></i>
                <span class="flash-title-text">FLASH SALE KHUNG GIỜ VÀ</span>
                <div class="flash-countdown-box">
                    <span class="cd-box cd-number">02</span>
                    <span class="cd-colon">:</span>
                    <span class="cd-box cd-number">15</span>
                    <span class="cd-colon">:</span>
                    <span class="cd-box cd-number">48</span>
                </div>
            </div>
            <a th:href="@{/products}" class="flash-see-all">Xem tất cả &gt;</a>
        </div>
        <div class="flash-scroll-container">
            <div class="flash-products-track" th:if="${not #lists.isEmpty(featuredProducts)}">
                <th:block th:each="stat, iter : ${featuredProducts}">
                    <a th:href="@{/products/{id}(id=${stat.product.id})}" class="flash-card-item">
                        <div class="flash-card-thumb">
                            <span class="flash-badge" th:if="${stat.hasDiscount}" th:text="'-' + ${stat.discountPercent} + '%'">-30%</span>
                            <span class="flash-badge flash-badge-soft" th:unless="${stat.hasDiscount}">HOT</span>
                            <img th:src="${stat.product.thumbnailUrl ?: 'https://placehold.co/300x300/f1f5f9/94a3b8?text=No+Img'}" th:alt="${stat.product.name}" class="flash-card-img">
                        </div>
                        <div class="flash-card-content">
                            <h3 class="flash-card-title" th:text="${stat.product.name}">Tên sản phẩm Flash Sale</h3>
                            <div class="flash-card-price" th:text="${@money.format(stat.product.price)}">249.000đ</div>
                            <div class="flash-progress-track">
                                <div class="flash-progress-fill" th:style="'width:' + ${stat.soldCount >= 100 ? 90 : (stat.soldCount * 100 / 100)} + '%'">.</div>
                            </div>
                            <span class="flash-sold-text">ĐÃ BÁN <th:block th:text="${stat.soldCount}">0</th:block></span>
                        </div>
                    </a>
                </th:block>
                <th:block th:each="stat, iter : ${featuredProducts}">
                    <a th:href="@{/products/{id}(id=${stat.product.id})}" class="flash-card-item" aria-hidden="true">
                        <div class="flash-card-thumb">
                            <span class="flash-badge" th:if="${stat.hasDiscount}" th:text="'-' + ${stat.discountPercent} + '%'">-30%</span>
                            <span class="flash-badge flash-badge-soft" th:unless="${stat.hasDiscount}">HOT</span>
                            <img th:src="${stat.product.thumbnailUrl ?: 'https://placehold.co/300x300/f1f5f9/94a3b8?text=No+Img'}" th:alt="${stat.product.name}" class="flash-card-img">
                        </div>
                        <div class="flash-card-content">
                            <h3 class="flash-card-title" th:text="${stat.product.name}">Tên sản phẩm Flash Sale</h3>
                            <div class="flash-card-price" th:text="${@money.format(stat.product.price)}">249.000đ</div>
                            <div class="flash-progress-track">
                                <div class="flash-progress-fill" th:style="'width:' + ${stat.soldCount >= 100 ? 90 : (stat.soldCount * 100 / 100)} + '%'">.</div>
                            </div>
                            <span class="flash-sold-text">ĐÃ BÁN <th:block th:text="${stat.soldCount}">0</th:block></span>
                        </div>
                    </a>
                </th:block>
            </div>
            </div>
        </div>
        </div>
    </div>
</section>

================================================================================
PHẦN 2 – CSS: FLASH DEAL STRIP (trong home.css)
================================================================================

/* ---------- Flash Deal Strip ---------- */
.flash-deal-section {
    padding: 24px 0;
    background: transparent;
}

.flash-deal-wrapper {
    background: var(--bg);
    border: 1px solid var(--line);
    border-radius: 12px;
    padding: 24px;
    box-shadow: var(--shadow-sm);
    overflow: hidden;
}

.flash-deal-header {
    background: var(--teal-soft);
    padding: 14px 24px;
    display: flex;
    align-items: center;
    justify-content: space-between;
    border-radius: 8px;
    margin-bottom: 20px;
    box-shadow: 0 2px 10px var(--teal-glow);
}

.flash-header-left {
    display: flex;
    align-items: center;
    gap: 12px;
    flex-wrap: wrap;
}

.flash-bolt-icon {
    color: var(--accent-red);
    font-size: 20px;
    animation: flash-pulse 1.5s ease-in-out infinite;
}

@keyframes flash-pulse {
    0%, 100% { transform: scale(1); opacity: 1; }
    50% { transform: scale(1.25); opacity: 0.85; }
}

.flash-title-text {
    font-size: 18px;
    font-weight: 800;
    color: var(--teal-deep);
    letter-spacing: -0.01em;
    text-transform: uppercase;
}

.flash-countdown-box {
    display: flex;
    align-items: center;
    gap: 4px;
    margin-left: 8px;
}

.cd-box {
    background: var(--teal-deep);
    color: #ffffff;
    font-size: 14px;
    font-weight: 800;
    padding: 3px 8px;
    border-radius: 4px;
    min-width: 28px;
    text-align: center;
}

.cd-colon {
    font-size: 14px;
    font-weight: 900;
    color: var(--teal-deep);
    padding: 0 1px;
}

.flash-see-all {
    font-size: 14px;
    font-weight: 600;
    color: var(--teal-deep);
    text-decoration: none;
    transition: color 0.2s ease, transform 0.2s ease;
    display: flex;
    align-items: center;
    gap: 4px;
}

.flash-see-all:hover {
    color: var(--teal-hover);
    transform: translateX(3px);
}

.flash-scroll-container {
    overflow: hidden;
    padding: 4px 0 16px 0;
}

.flash-products-track {
    display: flex;
    gap: 16px;
    animation: flash-scroll 30s linear infinite;
    width: max-content;
}

.flash-products-track:hover {
    animation-play-state: paused;
}

@keyframes flash-scroll {
    0% { transform: translateX(0); }
    100% { transform: translateX(-50%); }
}

.flash-card-item {
    width: 215px;
    flex-shrink: 0;
    background: var(--bg);
    border: 1px solid var(--line);
    border-radius: 8px;
    padding: 14px;
    text-decoration: none;
    display: flex;
    flex-direction: column;
    transition: all 0.3s cubic-bezier(0.165, 0.84, 0.44, 1);
    position: relative;
    box-shadow: var(--shadow-sm);
}

.flash-card-item:hover {
    border-color: var(--teal);
    box-shadow: inset 0 0 0 1px var(--teal), var(--teal-shadow);
    transform: translateY(-4px);
}

.flash-card-thumb {
    position: relative;
    width: 100%;
    aspect-ratio: 1 / 1;
    border-radius: 6px;
    overflow: hidden;
    background: var(--bg-alt);
    margin-bottom: 12px;
}

.flash-badge {
    position: absolute;
    top: 8px;
    left: 8px;
    z-index: 2;
    background: var(--accent-red);
    color: #ffffff;
    font-size: 11px;
    font-weight: 800;
    padding: 3px 7px;
    border-radius: 3px;
    box-shadow: 0 2px 4px rgba(239, 68, 68, 0.25);
}

.flash-badge.flash-badge-soft {
    background: linear-gradient(135deg, #ee4d2d 0%, #ff7337 100%);
    box-shadow: 0 2px 4px rgba(238, 77, 45, 0.3);
}

.flash-card-img {
    width: 100%;
    height: 100%;
    object-fit: cover;
    transition: transform 0.4s ease;
}

.flash-card-item:hover .flash-card-img {
    transform: scale(1.06);
}

.flash-card-content {
    display: flex;
    flex-direction: column;
    flex: 1;
}

.flash-card-title {
    font-size: 13px;
    font-weight: 500;
    color: var(--ink);
    line-height: 1.4;
    height: 36px;
    margin: 0 0 8px 0;
    display: -webkit-box;
    -webkit-line-clamp: 2;
    -webkit-box-orient: vertical;
    overflow: hidden;
    word-break: break-word;
}

.flash-card-price {
    font-size: 16px;
    font-weight: 800;
    color: var(--accent-red);
    margin-bottom: 10px;
}

.flash-progress-track {
    position: relative;
    height: 8px;
    background: var(--line-strong);
    border-radius: 10px;
    overflow: hidden;
    width: 100%;
}

.flash-progress-fill {
    height: 100%;
    background: linear-gradient(90deg, #12d8d3, var(--teal));
    border-radius: 10px;
    transition: width 0.3s ease;
}

.flash-sold-text {
    font-size: 10px;
    font-weight: 700;
    color: var(--muted);
    text-transform: uppercase;
    text-align: center;
    margin-top: 6px;
    letter-spacing: 0.02em;
}

================================================================================
PHẦN 3 – JAVASCRIPT: FLASH DEAL COUNTDOWN (trong home.js)
================================================================================

/* ---------- Flash Deal Countdown ---------- */
function initFlashCountdown() {
    const cdEl = document.querySelector('.cd-number');
    if (!cdEl) return;

    // Set end time: 3 hours from now
    let endTime = localStorage.getItem('flash_end_time');
    if (!endTime || parseInt(endTime) < Date.now()) {
        endTime = Date.now() + 3 * 60 * 60 * 1000;
        localStorage.setItem('flash_end_time', endTime);
    } else {
        endTime = parseInt(endTime);
    }

    function tick() {
        const now = Date.now();
        const diff = endTime - now;

        if (diff <= 0) {
            document.querySelectorAll('.cd-number').forEach(function(el) {
                el.textContent = '00';
            });
            return;
        }

        const totalSeconds = Math.floor(diff / 1000);
        const hours = Math.floor(totalSeconds / 3600);
        const minutes = Math.floor((totalSeconds % 3600) / 60);
        const seconds = totalSeconds % 60;

        const nums = document.querySelectorAll('.cd-number');
        if (nums.length >= 3) {
            nums[0].textContent = pad(hours);
            nums[1].textContent = pad(minutes);
            nums[2].textContent = pad(seconds);
        }

        setTimeout(tick, 1000);
    }

    tick();
}

function pad(n) {
    return n < 10 ? '0' + n : String(n);
}

================================================================================
PHẦN PHỤ – MINI CARD FLASH SALE (trong hero-side-grid, HTML)
================================================================================

<!-- Nằm trong hero-side-grid trong section hero-banner-section -->
<a th:href="@{/products}" class="hero-side-card flash">
    <div class="hsc-icon"><i class="fas fa-bolt"></i></div>
    <div class="hsc-text">
        <div class="hsc-title">Flash Sale</div>
        <div class="hsc-sub">Giảm sốc <strong>70%</strong></div>
    </div>
    <i class="fas fa-arrow-right hsc-arrow"></i>
</a>

/* CSS cho hero-side-card.flash (đã có trong home.css):
.hero-side-card.flash .hsc-icon {
    background: linear-gradient(135deg, #ffd166, #ee5a24);
    color: #fff;
} */

================================================================================
PHẦN PHỤ – MARQUEE BAR (thanh chạy trên cùng có nhắc Flash Sale)
================================================================================

<!-- HTML marquee-bar (trong index.html, top bar) -->
<div class="marquee-bar">
    <div class="marquee-track">
        <div class="marquee-content">
            <!-- ... các span khác ... -->
            <span><i class="fas fa-fire" style="color:#ef4444"></i> Flash Sale 50% off - Chỉ hôm nay!</span>
            <!-- ... -->
        </div>
    </div>
</div>

/* CSS marquee-bar (trong home.css) */
/* Marquee đã có sẵn trong home.css phía trên */

