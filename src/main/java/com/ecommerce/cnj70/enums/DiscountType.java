package com.ecommerce.cnj70.enums;

/**
 * Loại giảm giá: PERCENT (phần trăm), AMOUNT (số tiền cố định)
 */
public enum DiscountType {
    PERCENT,  // Giảm theo % (VD: giảm 10%)
    AMOUNT    // Giảm theo số tiền cố định (VD: giảm 50.000đ)
}
