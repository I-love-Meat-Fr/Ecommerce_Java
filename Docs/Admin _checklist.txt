Tôi là **Hoàn**, phụ trách phần **ADMIN** của project.

Tôi đã đính kèm source code, workbook phân chia công việc và các tài liệu liên quan. Hãy **đọc trực tiếp các file đã đính kèm**, không yêu cầu tôi nhắc lại nội dung.

Tôi đã thực hiện một vòng audit trước và có kết luận bên dưới. Hãy xem kết luận này là **baseline hiện tại của roadmap**, sau đó kiểm tra lại với source/workbook để xác nhận trước khi tôi bắt đầu code.

---

# KẾT LUẬN AUDIT HIỆN TẠI

Roadmap cũ **CHƯA ĐỦ để triển khai an toàn**, dù đã bao phủ 22/22 task trong workbook.

* Không thiếu nhóm nghiệp vụ chính.
* Thiếu 13 task kỹ thuật bắt buộc.
* 7 Phase cũ được điều chỉnh thành **8 Phase triển khai + Gate 0**.
* Có các dependency giữa Hoàn, Quốc Anh, Mạnh Quân và nhóm.
* Workbook ghi toàn bộ task là "Chưa làm", trong khi source đã có bản đầu của User, Category và Dashboard nhưng chưa được test.
* `mvn test` từng BUILD SUCCESS nhưng project chưa có `src/test`, nên chưa chứng minh chức năng đúng.

---

# ROADMAP ĐỀ XUẤT HIỆN TẠI

```text
Gate 0 — Chốt contract nghiệp vụ/dependency
        ↓
Phase 1 — Admin Access
        ↓
Phase 2 — Admin Shell
        ↓
Phase 3 — User Management
        ↓
Phase 4 — Category Management
        ↓
Phase 5 — Shop Approval
        ↓
Phase 6 — Admin Order
        ↓
Phase 7 — Dashboard
        ↓
Phase 8 — Integration & Testing

```

---

# GATE 0 — CHỐT CONTRACT

Trước khi code phải chốt:

1. Cách tạo/test tài khoản ADMIN hợp lệ.
2. Có thực hiện Role Management hay không.
3. Shop có những trạng thái nào.
4. Cách biểu diễn Reject Shop.
5. Shop chưa được duyệt có được tạo/bán Product hay không.
6. Cách đọc Order đa Shop.
7. Công thức Revenue.

Dependency:

* Quốc Anh
* Mạnh Quân
* Hoàn
* Nhóm

**Không code các vấn đề chưa được nhóm thống nhất.**

---

# PHASE 1 — ADMIN ACCESS

Mục tiêu:

Hoàn thiện quyền truy cập và security cần thiết cho Admin.

Task:

```text
1.1 Admin account
1.2 Role-safe registration
1.3 Login redirect theo role
1.4 Chặn LOCKED user tại web login
1.5 Chặn JWT của LOCKED user
1.6 Security tests

```

Dependency chính:

**Quốc Anh**

Done khi:

* ADMIN vào được `/admin/dashboard`
* Customer/Vendor không vào được Admin
* Role không phù hợp nhận 403
* User LOCKED không login được
* JWT của user LOCKED không được chấp nhận
* Không thể tự đăng ký ADMIN
* Có test Security thật

Lưu ý:

Hoàn không tự ý refactor toàn bộ Auth/JWT. Đây là phần core của Quốc Anh.

---

# PHASE 2 — ADMIN SHELL

Task:

```text
2.1 Admin layout
2.2 Admin sidebar/navigation
2.3 Flash message
2.4 Error UI
2.5 Logout/navigation

```

Done khi:

* Các trang Admin dùng layout thống nhất.
* Sidebar truy cập được các module Admin.
* Logout hoạt động.
* Error/flash message hiển thị đúng.
* Không còn fragment/layout sai hoặc mang nội dung Vendor.

---

# PHASE 3 — USER MANAGEMENT

Task:

```text
3.1 User List
3.2 User Detail
3.3 Lock User
3.4 Unlock User
3.5 Guard nghiệp vụ
3.6 Role Management — CONDITIONAL
3.7 User tests

```

Done khi:

* `/admin/users` hoạt động.
* Detail hoạt động.
* Lock/Unlock cập nhật MongoDB.
* Không cho Admin tự khóa.
* Không khóa Admin trái nghiệp vụ.
* ID không tồn tại được xử lý.
* Security behavior đúng.
* Có test thật.

Dependency:

**Quốc Anh** đối với Lock/JWT/Security.

---

# PHASE 4 — CATEGORY MANAGEMENT

Task:

```text
4.1 Category List
4.2 Create Category
4.3 Category validation
4.4 Edit Category
4.5 Delete Category
4.6 Product reference guard
4.7 Category tests

```

Done khi:

* List hoạt động.
* Create hoạt động.
* Không cho tên rỗng.
* Không cho category trùng.
* Edit hoạt động.
* ID không tồn tại được xử lý.
* Không xóa Category đang được Product tham chiếu.
* MongoDB không tạo dữ liệu tham chiếu hỏng.
* Có test thật.

Dependency:

**Mạnh Quân** đối với Product reference.

---

# PHASE 5 — SHOP APPROVAL

Task:

```text
5.1 Pending Shop List
5.2 Shop Detail
5.3 Approve Shop
5.4 Reject Shop
5.5 Active/Inactive
5.6 State transition validation
5.7 Shop tests

```

Trước khi code phải xác nhận:

```text
verified
active
rejected

```

được sử dụng như thế nào.

Không tự ý tạo thêm enum/status mới nếu nhóm chưa thống nhất.

Done khi:

* Pending list đúng.
* Detail đúng.
* Approve cập nhật đúng DB.
* Reject cập nhật đúng DB.
* Active/Inactive đúng nghiệp vụ.
* Transition không hợp lệ bị chặn.
* Vendor quan sát/tuân thủ trạng thái đúng.
* Có test thật.

Dependency:

**Mạnh Quân + nhóm.**

---

# PHASE 6 — ADMIN ORDER

Admin chỉ có quyền **READ / TRACK** trong phạm vi của Hoàn.

Task:

```text
6.1 Order List
6.2 Filter theo Status
6.3 Order Detail
6.4 Map Customer
6.5 Map Shop
6.6 Map Items
6.7 Order tests

```

Không triển khai:

```text
Admin sửa Order
Admin hủy Order
Admin xác nhận Order

```

nếu workbook không yêu cầu.

Đặc biệt phải kiểm tra Order hiện tại trước khi code.

Nếu Order thực sự là multi-shop nhưng `shopId/shopName` không được gán đúng:

* Không tự ý sửa Order core.
* Báo Quốc Anh.
* Hoàn chỉ xây phần read view theo read model đã được thống nhất.

Done khi:

* Admin xem được toàn bộ Order.
* Detail hiển thị đúng dữ liệu.
* Multi-shop không làm sai UI.
* Filter status hoạt động.
* Không có endpoint thay đổi trạng thái Order.
* Có test thật.

Dependency:

**Quốc Anh.**

---

# PHASE 7 — DASHBOARD

Chỉ làm sau khi domain chính đã ổn định.

Task:

```text
7.1 Total User
7.2 Total Shop
7.3 Total Product
7.4 Total Order
7.5 Revenue
7.6 Quick Links
7.7 Recent Activities — OPTIONAL
7.8 Dashboard tests

```

Đặc biệt kiểm tra:

* `activeVendors` có thực sự đếm Vendor đang active hay chỉ đếm role Vendor.
* Total Shop lấy dữ liệu đúng.
* Total Product lấy dữ liệu đúng.
* Total Order lấy dữ liệu đúng.
* Revenue có đúng công thức nghiệp vụ không.

Done khi:

* Dashboard hiển thị đúng dữ liệu MongoDB.
* Có fixture/dữ liệu test để đối chiếu.
* Công thức Revenue được ghi rõ.
* Quick Links hoạt động.
* Có test thật.

Dependency:

* User → Quốc Anh
* Shop/Product → Mạnh Quân
* Order/Revenue → Quốc Anh

---

# PHASE 8 — INTEGRATION & TESTING

Không coi đây là lần đầu tiên test project.

Các Phase trước phải có test riêng.

Phase 8 là regression/integration cuối.

Task:

```text
8.1 Full test suite
8.2 Controller tests
8.3 Service tests
8.4 Security tests
8.5 MongoDB/runtime verification
8.6 Role/403 verification
8.7 Regression
8.8 Final Admin checklist

```

Done khi:

```bash
mvn test

```

thực sự chạy test source.

Sau đó:

```bash
mvn spring-boot:run

```

và kiểm tra runtime.

Phải kiểm tra:

* Admin
* Customer
* Vendor
* 403
* LOCKED User
* User
* Category
* Shop
* Order
* Dashboard
* MongoDB
* Error handling
* Các chức năng cũ không bị hỏng

---

# CÁC TASK KỸ THUẬT BẮT BUỘC PHẢI ĐƯỢC GIỮ

Không được bỏ 13 task sau nếu source/workbook vẫn xác nhận chúng cần thiết:

1. Admin account hợp lệ.
2. Chặn `role=ADMIN` khi registration.
3. Login redirect theo role.
4. Chặn LOCKED tại web login.
5. Chặn JWT của LOCKED user.
6. Không cho Admin tự khóa/khóa Admin trái nghiệp vụ.
7. Xử lý ID không tồn tại.
8. Error 400/error view.
9. Category validation + duplicate.
10. Category deletion integrity.
11. Shop state transition.
12. Order multi-shop + Revenue contract.
13. Controller/Service/Security tests.

---

# NGUYÊN TẮC PHÂN QUYỀN

Phải giữ đúng phạm vi:

### Hoàn

* Admin
* User Management
* Category
* Shop Approval
* Admin Order read/tracking
* Dashboard

### Quốc Anh

* Authentication
* JWT
* Security core
* User core
* Order core

### Mạnh Quân

* Shop core
* Product
* Vendor

Nếu Admin cần thay đổi code của người khác:

```text
KHÔNG TỰ Ý SỬA
        ↓
Xác định dependency
        ↓
Thông báo người phụ trách
        ↓
Thống nhất contract
        ↓
Mới code

```

---

# QUY TẮC "KHÔNG ĐỂ AI DẮT"

Trong toàn bộ quá trình triển khai, AI phải phân biệt:

```text
[FACT]
Thông tin xác nhận trực tiếp từ source/workbook.

[INFERENCE]
Suy luận từ source.

[RECOMMENDATION]
Đề xuất của AI.

[DEPENDENCY]
Phần cần người khác xác nhận/sửa.

[BLOCKER]
Vấn đề khiến Task không thể tiếp tục an toàn.

```

Không được biến:

```text
[RECOMMENDATION]

```

thành:

```text
[REQUIREMENT]

```

Không được tự ý mở rộng scope.

Không được tự ý refactor architecture.

Không được tự ý sửa module của thành viên khác.

Không được đoán source code.

Nếu chưa đủ thông tin:

> Dừng và yêu cầu kiểm tra source liên quan.

---

# QUY TẮC TASK

Mỗi Task phải đi theo flow:

```text
ANALYZE
↓
UNDERSTAND
↓
PLAN
↓
CODE
↓
RUN
↓
TEST
↓
HOÀN TỰ KIỂM TRA
↓
HOÀN XÁC NHẬN
↓
COMMIT
↓
DONE

```

AI **không được tự đánh dấu DONE**.

Chỉ khi tôi nói:

> `TASK X.X HOÀN THÀNH`

mới chuyển Task.

---

# QUY TẮC COMMIT

Mỗi Task hoàn thành một chức năng chính thì tạo commit riêng.

Ví dụ:

```bash
git status
git add <đúng các file liên quan>
git commit -m "feat(admin): <task>"
git push origin admin

```

Commit phải phản ánh đúng Task.

Không dùng:

```text
update
final
done
fix
admin

```

một cách chung chung.

Không tạo commit giả chỉ để đánh dấu tiến độ.

---

# VIỆC BẠN CẦN LÀM BÂY GIỜ

**Chưa được code.**

Hãy sử dụng roadmap trên làm baseline và kiểm tra lần cuối với **source code + workbook đã đính kèm**.

Tôi muốn bạn trả lời đúng 6 phần:

## 1. FINAL VALIDATION

Roadmap 8 Phase + Gate 0 hiện tại:

```text
[VALID]
hoặc
[NEEDS CHANGE]

```

Nếu cần thay đổi, chỉ ra chính xác thay đổi nào và **bằng chứng từ source/workbook**.

---

## 2. FINAL PHASE MAP

Hiển thị:

```text
Gate 0
→ Phase 1
→ Phase 2
→ Phase 3
→ Phase 4
→ Phase 5
→ Phase 6
→ Phase 7
→ Phase 8

```

Mỗi Phase ghi:

* Mục tiêu
* Task
* Dependency
* Done criteria

---

## 3. 22 TASK MAPPING

Đảm bảo 22 task trong workbook đều được mapping:

```text
22 task
→ Phase
→ Task nhỏ
→ Dependency
→ Commit

```

Không được bỏ task nào.

---

## 4. 13 TECHNICAL TASK

Xác nhận 13 task kỹ thuật đã được đặt vào Phase nào.

Nếu task nào không còn cần thiết, phải chứng minh bằng source/workbook.

---

## 5. BLOCKER LIST

Liệt kê tất cả blocker trước khi Hoàn có thể bắt đầu code.

Phân loại:

```text
BLOCKER — cần Quốc Anh
BLOCKER — cần Mạnh Quân
BLOCKER — cần nhóm
BLOCKER — Hoàn có thể tự xử lý

```

---

## 6. FINAL DECISION

Cuối cùng chỉ trả lời:

```text
ROADMAP ADMIN:
[READY TO IMPLEMENT]
hoặc
[NOT READY]

```

Nếu READY:

> Task đầu tiên cần làm là gì?

Nhưng **không triển khai Task đó**.

Chờ tôi nói:

> **BẮT ĐẦU PHASE 1**

Mới bắt đầu hướng dẫn implementation.

Mục tiêu của tôi không phải để AI làm Admin thay tôi.

Mục tiêu là:

> **Tôi hiểu → tôi code → tôi test → tôi commit → tôi hoàn thành từng chức năng.**

Không sửa source trong bước audit này.
